# Redirect all routes to index.html for client-side routing
# Netlify will serve static files (JS, CSS, images) directly if they exist
# This redirect only applies to routes that don't match actual files
/*    /index.html   200
